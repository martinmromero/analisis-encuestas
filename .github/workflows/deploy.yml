name: Notify Push

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    
    steps:
      - name: Notify push successful
        run: |
          echo "✅ Push exitoso a GitHub"
          echo "El servidor sincronizará automáticamente en 1-2 minutos"
